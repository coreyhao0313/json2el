{
  "name": "json2el",
  "version": "1.0.0",
  "description": "make element with json and bind data",
  "main": "dist/json2el.js",
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  },
  "scripts": {
    "build": "rm -rf dist/ && babel src -d dist",
    "test": "echo \\\"Error: no test specified\\\" && exit 1"
  },
  "author": "coreyhao",
  "license": "ISC"
}
